name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build with environment variables
      run: npm run build
      env:
        VITE_SUPABASE_URL: "https://supywgkoghcphlynktmr.supabase.co"
        VITE_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN1cHl3Z2tvZ2hjcGhseW5rdG1yIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjY4NTYzNjQsImV4cCI6MjA4MjQzMjM2NH0.b_6COwOKkt2sWOuzM42W-LZfraPJJSKXhax69IUGwWc"
        VITE_ADMIN_CODE: "Sarah2017"
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
